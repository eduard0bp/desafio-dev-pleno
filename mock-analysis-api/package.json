{
  "name": "mock-review-analysis-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API fake do Falaê para o desafio de processamento assíncrono de avaliações.",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.0"
  }
}
